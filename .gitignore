# Build output
bin/
obj/
publish/

# Runtime data (per-repo run logs/summaries)
.ai-runner/
**/.ai-runner/

# Scratch
scratch_run.log
*.user
.vs/

# Local secret overrides (never committed) — API keys live here, not in appsettings.json
appsettings.Local.json

# Autodev orchestrator secrets & run logs
.env
tools/autodev-orchestrator/appsettings.json
docs/autodev/logs/*
!docs/autodev/logs/.gitkeep
