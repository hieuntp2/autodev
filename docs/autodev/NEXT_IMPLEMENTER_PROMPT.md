# Next Implementer Task — Daily Pixel Garden

> Generated by autodev-orchestrator on 2026-07-03 23:57. This file is the handoff from the OpenAI Creative Director to the code implementer (Claude CLI / Codex / autodev). Execute exactly one task, then update the status files.

## Project context

- Daily Pixel Garden is a cozy pixel-art web world (Angular + PixiJS) that grows a little every day.
- The garden app lives at `apps/daily-pixel-garden` — **it does not exist yet; this task may include creating it.**
- Current plan: `docs/autodev/CURRENT_PLAN.json` (date 2026-07-03, theme: "Planting the first seed").
- Creative direction: Bring the garden into existence: a minimal Angular + PixiJS world with a first living sprout and a sky that knows what time it is.
- Plan constraints:
  - Keep each change small
  - Do not add backend, database, or auth
  - Do not add paid or external assets — draw with PixiJS primitives
  - Never put API keys in the frontend

## Task DPG-20260703-001: Scaffold the Daily Pixel Garden app (Angular + PixiJS)

- Type: foundation | Priority: high | Size: medium

### Implementation prompt

Create a minimal Angular workspace at apps/daily-pixel-garden with a single standalone component hosting a full-window PixiJS canvas. Add pixi.js as the only extra dependency. Initialize the Pixi Application in ngAfterViewInit, destroy it in ngOnDestroy, and render a plain dark-soil background color so the canvas is visibly alive. Configure crisp pixel rendering (roundPixels, nearest scale mode). Add a short README.md in the app folder explaining npm start / npm run build. No routing, no backend, no tests scaffolding beyond the Angular defaults.

### Acceptance criteria

- [ ] npm install succeeds in apps/daily-pixel-garden
- [ ] npm run build succeeds
- [ ] npm start serves a full-window canvas with the soil background
- [ ] No console errors in the browser

## Files likely to inspect

- `docs/autodev/CURRENT_PLAN.json` — full plan and task statuses
- `docs/autodev/DAILY_LOG.md` — story so far
- `apps/daily-pixel-garden/` — to be created (keep it a minimal Angular workspace with a PixiJS canvas)

## Verification

The garden app has no package.json yet. After scaffolding, `npm install` and `npm run build` must succeed inside `apps/daily-pixel-garden`. Do not invent other commands.

Then start the dev server, open the app in a browser, and confirm the acceptance criteria and that the console has no errors.

## After completion (required)

1. In `docs/autodev/CURRENT_PLAN.json`, set task `DPG-20260703-001` `"status"` to `"completed"` and `"completed_at"` to the current ISO timestamp. If the task turned out infeasible, set `"skipped"` instead and explain why in the daily log.
2. Append a short dated entry to `docs/autodev/DAILY_LOG.md` describing what changed in the garden (one or two cozy sentences plus a technical note).
3. Do not start other tasks, do not call OpenAI, do not edit `docs/autodev/plans/` history. Keep the change small.
